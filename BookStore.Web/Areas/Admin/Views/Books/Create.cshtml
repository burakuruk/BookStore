@using BookStore.Web.Services
@model BookCreateUpdateDto
@{
    ViewData["Title"] = "Kitap Oluştur";
    var categories = ViewBag.Categories as List<CategoryDto> ?? new();
}
<div class="container-fluid py-2">
    <h2>Kitap Oluştur</h2>
    <form asp-area="Admin" asp-controller="Books" asp-action="Create" method="post" enctype="multipart/form-data" class="mt-3">
        <div class="row g-3">
            <div class="col-md-8">
                <div class="mb-3">
                    <label class="form-label">Başlık</label>
                    <input class="form-control" asp-for="Title" />
                </div>
                <div class="mb-3">
                    <label class="form-label">Yazar</label>
                    <input class="form-control" asp-for="Author" />
                </div>
                <div class="mb-3">
                    <label class="form-label">Fiyat</label>
                    <input class="form-control" asp-for="Price" />
                </div>
                <div class="mb-3">
                    <label class="form-label">Kategori</label>
                    <select class="form-select" asp-for="CategoryId">
                        @foreach (var c in categories)
                        {
                            <option value="@c.Id">@c.Name</option>
                        }
                    </select>
                </div>
            </div>
            <div class="col-md-4">
                <div class="mb-3">
                    <label class="form-label">Kapak Görseli</label>
                    <input type="file" class="form-control" name="image" accept="image/*" />
                </div>
            </div>
        </div>
        <button class="btn btn-primary" type="submit">Kaydet</button>
        <a class="btn btn-secondary" asp-action="Index">Geri</a>
    </form>
</div>


